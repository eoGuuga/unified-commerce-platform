# GTSoftHub — Roadmap Vivo

> **Doc de referência entre sessões.** Organizado por **decisão e ação**, não por lista.
> Leia o topo pra agir; o Apêndice é a enciclopédia. Atualize os status ao concluir.
>
> **Legenda:** status `⬜ a fazer` · `🔄 em andamento` · `✅ feito` · `🚫 bloqueado (mundo-real)` — gravidade `🔴 morde cedo` · `🟡 importante` · `🟢 pode esperar` — dono `👤 mundo-real (dono + profissional)` · `🔧 técnico (nós)`.
>
> _Última atualização: 2026-07-06 (auditoria empírica de segurança — 7 blocos)._

---

## 🧭 Estado atual (leia isto primeiro)

Prod (servidor `/opt/gtsofthub`, HEAD `98e1827`, site 200) roda: **multi-tenancy RLS + segurança HIGH fechada + PDV/venda de balcão + admin produtos&estoque + PIX no bot**. **⚠️ `main` está À FRENTE de prod (prod=`98e1827` ≠ origin/main): há 3 fixes de SERVIÇO mergeados ainda NÃO deployados** (retry, escalação enriquecida, remoção de método morto — ver "Baseline de falhas whatsapp/products" no backlog); chegam no próximo deploy de código. **1 tenant real** (a doceria "Loucas por Brigadeiro"). Quatro frentes prontas em branch, **esperando o mundo-real**: cupom de venda (teste na bobina da mãe), pagamentos Asaas (**Rota 1 PIX provada AO VIVO ✅ 2026-07-07**; falta CNPJ+prod = Fase 4), bot WhatsApp (**Meta ✅ criada**, falta ativar). **O gargalo central não é código — é os 3 destravamentos de mundo-real (Asaas, Meta, CNPJ):** o código está adiante do que o mundo-real permite ligar.

> ### 🔴 BLOQUEADORES ATIVOS — ler ANTES de mergear qualquer código
> Dois achados de 2026-07-07 (a régua "verifica, não confia" pescou os dois contra suposições nossas). O #1 já foi **resolvido**; o #2 **segue ativo** — pré-requisito, não nota de rodapé:
>
> 1. **✅ LANDMINE DESARMADA (2026-07-07).** O auto-deploy `.github/workflows/deploy-prod.yml` tinha `on: push` (backend/frontend) que subia container com **env incompleto → quebraria o boot em prod sozinho** (e parava o container bom antes). **Desarmado:** trigger trocado para `workflow_dispatch` (só manual, **nunca dispara sozinho**) + cabeçalho explicativo no arquivo; arquivo **preservado como referência** (reversível por `git revert`). Merges de backend/frontend **não disparam mais** deploy automático. *(Melhoria futura OPCIONAL, não urgente: reativar como auto-deploy CORRETO — mesmo compose/env completo + health-check com rollback + teste em staging.)*
> 2. **🔴 Pagamentos NÃO são dormentes — NÃO podem ir pra main até a Fase 4.** O `payments.service` na branch já **re-roteia o MP** via `resolveForCharge` **fail-closed** (exige credencial por-tenant na tabela nova `tenant_payment_credentials`, **sem fallback pro env**). Mergear+deployar sem a migration `1752200000000` + a credencial MP da doceria **QUEBRA o PIX que funciona hoje** — não é "não-ativa-antes-da-hora", é **quebrar o que já funciona**. A Fase 4 é quem trata migration + credenciais por-tenant + a transição do MP existente. *(Correção da leitura anterior do dono, verificada na branch.)*

---

## 🌍 Ações do mundo real (só o dono resolve, fora do código)

### Destravamentos (na ordem de impacto)
1. **✅ 👤 Chamado ao Asaas — ENVIADO E RESOLVIDO (chamado 1356594, Isadora/Integrações, 2026-07-07).** O time liberou o PIX de subconta White Label no sandbox. **🎉 Rota 1 da Fase 2 PROVADA AO VIVO (2026-07-07)** — round-trip PIX completo com subconta **REAL** (`accountId 1e694b22`): cobrança **400→200** (gate destravado) + QR real gerado + pagamento recebido + **webhook roteou pra subconta certa (roteamento reverso ao vivo)** + pedido fechou sozinho (`confirmado`→`paid`) + **Risco A confirmado AO VIVO** (chave "só cobrança" → **403 no saque**: recebe mas não saca). O diferencial central do produto, provado de ponta a ponta. *(Sandbox; banco de teste limpo pós-prova; reversível.)*
2. **✅ 🎉 Bot WhatsApp (Cloud API) — PROVADO FUNCIONAL PONTA A PONTA (2026-07-07). Falta só o NÚMERO REAL pra entrega.** Conta Meta criada; código do webhook **mergeado + deployado em prod** (`e7eadb1`); bot **armado e correto** (`WHATSAPP_PROVIDER=cloud_api` + as 5 vars no `.env` **e no compose** — o compose não mapeava as novas vars, consertado `e7eadb1`; webhook **verificado pela Meta ✅**; **WABA subscrita no app certo "botwhats"** — o Claude Code consertou: estava subscrita só no app interno "WA DevX 1P" da Meta, por isso os eventos iam pro painel, não pro nosso webhook; número de teste **registrado** `/register` → `platform_type: CLOUD_API`). **Round-trip PROVADO nos logs:** um "oi" do celular do dono → **inbound recebido → HMAC → allowlist → bot processou** (conversa criada, telefone **mascarado `55****10`** — o fix de PII funcionando) **→ resposta enviada → Meta ACEITOU (2xx)**. **O ÚNICO gargalo é a ENTREGA outbound do NÚMERO DE TESTE** da Meta: ele **recebe inbound mas não entrega outbound** (o hello_world e a resposta do bot foram aceitos pela Meta 2xx mas não chegaram no celular). **É limitação do número de teste, NÃO bug nosso** — o código está provado correto.
   - **▶️ ATIVAÇÃO REAL (mundo-real + sessão de config):** conectar um **NÚMERO REAL dedicado** (chip/número só do bot, **NÃO o pessoal do dono `5511978118610`**) → verificar → registrar (`/register`) → aí o outbound entrega. É a **Etapa 2 da Meta**. Todo o resto está pronto (código, webhook, subscrição da WABA, allowlist, registro) — **só falta trocar o número de teste pelo real**.
   - **Estado no `.env`/prod:** `WHATSAPP_PROVIDER=cloud_api` + as 5 vars (token permanente, App Secret, verify token). PIN do número de teste (verif. 2 etapas) guardado no Bitwarden pelo dono. *(⚠️ Finding: o antigo "usar `MERCADOPAGO_ACCESS_TOKEN=TEST-` na prova" QUEBRA o boot em prod — o guard S1 rejeita `TEST-` em prod. Em prod o MP fica `APP_USR-` real → testar só CONVERSA, não finalizar compra.)*
   - 🐛 **Bug não-bloqueante:** `Failed to track message metrics` (`WhatsAppAnalyticsService`) — erro não-fatal no tracking (a resposta sai mesmo assim); corrigir depois (o app-alert dispara nele).
3. **⬜ 👤 Abrir o CNPJ (com o contador).** **Destrava:** **produção de pagamentos** — a conta-plataforma Asaas EXIGE CNPJ (é a Fase 4). **Estado:** pendente. Sem CNPJ, pagamentos ficam presos ao sandbox. (Ver a pauta do contador abaixo antes de decidir MEI vs empresa.)

### 📋 Pauta pro CONTADOR (a conversa que o CNPJ exige)
- **Enquadramento:** você é **SaaS que cobra mensalidade** (fee) ou você **"toca" o dinheiro** do lojista? Muda tudo (obrigações, risco).
- **CNAE** certo para a atividade (software/plataforma).
- **Obrigação fiscal de intermediar recebimento de terceiros** — gera obrigação sua? Nota/imposto sobre **a sua receita** (a mensalidade), não sobre a venda do lojista.
- **MEI vs empresa:** o MEI tem **teto (~R$81k/ano)** — a plataforma escala além disso? Se sim, já nascer empresa.

### ⚖️ Pauta pro ADVOGADO (para quando houver lojistas de verdade — **NÃO agora**)
- **Responsabilidade perante o cliente final** quando um pagamento some/atrasa (sua exposição como facilitador; o white-label tem responsabilidade solidária → preferir subconta PADRÃO).
- **Termos de uso:** dono↔lojista (SLA, fee, responsabilidade, dados) **e** loja↔cliente final.
- **LGPD:** você é **controlador ou operador** dos dados dos clientes dos lojistas? DPA com cada lojista; direito de exclusão (hoje é stub).
- **KYC/AML:** quem vet­a o lojista? (subconta pode ser vetor de lavagem — papel da plataforma).

---

## 🔧 Técnico — ATACAR EM BREVE (nós, priorizado)

Os 3 pontos cegos 🔴 são **baratos de mitigar e caros de ignorar** — fazer primeiro.

1. **✅ 🔴 🔧 Backup — CONSERTADO e validado (2026-07-06)** ([runbook](../superpowers/plans/2026-07-06-backup-fix.md)). Diagnóstico: prod estava **sem backup automático** (cron nunca agendado no path novo). Feito, na régua de servidor (só-leitura em prod, sem restart, tudo reversível): backup fresco → **restore provado byte-fiel** no `ucm-postgres-test` (contagens batem com prod) → script corrigido (`BACKUP_DIR` **derivado do local do script = mata o F20 na raiz** + pre-flight de container + tmp→final atômico anti-gzip-vazio) mergeado na `main` e deployado (servidor == origin/main, sem drift) → **cron versionado** em `/etc/cron.d/gtsofthub-backup` (03:00 BRT, rotação 14d) → 1 run validado (dump no path certo, log, rotação sã). **Fast-follow ABERTO 🟡:** cópia **off-site** dos dumps (hoje no mesmo disco da VPS = não protege contra a VPS morrer). *(Achado colateral ABERTO 🔴: watchdog + 3 crons de monitoramento mortos apontando pro `/opt/ucm` velho → frente de Observabilidade abaixo.)*
2. **✅ 🔴 👤+🔧 Custódia da `ENCRYPTION_MASTER_KEY` — RESOLVIDA (2026-07-06).** As 3 chaves críticas (`ENCRYPTION_MASTER_KEY` KEK, `ENCRYPTION_KEY` legado v1, `JWT_SECRET`) — antes só no `.env` da VPS — foram **custodiadas off-server pelo dono num gerenciador de senhas real (Bitwarden)** e **verificadas por fingerprint** (sha256 da cópia bateu com o do servidor, sem a chave passar por lugar nenhum). Risco irreversível fechado: se a VPS morrer, as chaves (que nem o backup do banco desfaz) estão salvas. **Rotação = Fase 4 (KMS: chave sai do `.env` p/ cofre gerenciado; depende de CNPJ/prod).** **Higiene ABERTA 🟢:** `deploy/.env.bak-20260703-131517` (cópia de segredos no MESMO disco, sobra da faxina) — remover **depois**, com cuidado (arquivo de segredo, passa pelo dono); NÃO agora.
3. **✅ 🔴 🔧 Observabilidade — RESOLVIDO (2026-07-07)** ([runbook](../superpowers/plans/2026-07-06-observability.md)). Raiz: a faxina de path matou a automação de monitoramento + prod estava **cego na entrega** (nada chegava ao dono). **Fechado — 4 camadas no ar, todas provadas:**
   - ✅ **Canal Telegram LIGADO** — achei um bot de teste **já criado** (aplicando a régua "já existe?"), reaproveitado (vars no `deploy/.env`, cópia arquivo→arquivo sem expor valor), provado ponta a ponta.
   - ✅ **Watchdog CONSERTADO + versionado + NO AR com alerta** — repontado (path derivado); recovery corrigido (`-p deploy --env-file .env`, igual ao `deploy.sh` — o velho usava `env.prod` → teria FALHADO); checks → **HTTPS do domínio** (batiam em `http://localhost`/301 → 3 bugs: 1 falso-positivo + 2 falso-negativos); **avisa no Telegram** ao cair/recuperar (fim do recovery silencioso); versionado em `deploy/scripts/ucm-watchdog.sh` + `deploy/systemd/`; timer active/enabled, **3 ciclos automáticos `all_good` sem recovery**. *(Lição registrada: um blip real — rodei contra prod com checks não-verificados e reiniciou containers 2x → virou a régua "verificar COMPORTAMENTO antes de agir".)*
   **As 3 pontas que faltavam — TODAS fechadas (2026-07-07):**
   - ✅ **netdata → Telegram (2026-07-07)**: ligado o método Telegram nativo (mesmo bot do watchdog), escopo **só role `sysadmin`** (disco/inode/ram/memória — sem ruído de CPU; os alarmes chatos já são `to: silent`), config **HOT** em `/etc/netdata/health_alarm_notify.conf` (server-only, token `640 root:netdata`) + template versionado (`deploy/netdata/health_alarm_notify.telegram.example`, sem segredo). Teste nativo (warning/critical/clear) chegou no Telegram; netdata não reiniciou. Anti-spam: thresholds+histerese+só-transição.
   - ✅ **3 crons mortos REMOVIDOS (2026-07-07)** — `monitor.sh` (monitor geral → coberto por watchdog+netdata), `disk-alert.sh` (→ coberto por netdata), `security-alert.sh` (o log provou: era só um **contador de bans do fail2ban**, não proteção — o fail2ban está **ativo**, 2 jails `sshd`+`nginx-http-auth`, banindo). Removidos do crontab do `ubuntu` (backup em `/home/ubuntu/crontab.bak-*`; `/etc/cron.d`+watchdog+netdata **intocados**); `monitor.log` truncado. **Opcional pra depois (NÃO é gap):** se quiser visibilidade de segurança, um **resumo DIÁRIO** do fail2ban ou alerta só em **anomalia** (pico de bans) — nunca o contador de 15min (=spam, 1-3 bans/15min é ruído de fundo normal); o fail2ban protege independente disso.
   - ✅ **Alerta de app Tier 1-lite (2026-07-07)**: cron `*/5` versionado (`deploy/scripts/app-alert.sh` + `deploy/cron/gtsofthub-app-alert`) varre o log do backend por `"level":"error"` (o formato JSON REAL — grep por `ERROR` maiúsculo pegaria NADA, achado no diagnóstico) na janela `--since 6m` (1min de sobreposição → nunca perde) → Telegram. Anti-flood (1 msg c/ contagem + distintas) + sanitizado (só context+message, truncado 200c). Provado com os 5 erros reais (dry-run→envio→run silencioso→cron automático limpo). **Achou de brinde:** o bug do audit-log de login falhando (registrado em Segurança). **Tier 2** (notificador semântico in-app + APM Loki/Sentry) registrado pra depois.
   - *(Edge achado na revisão: o watchdog pode disparar recovery no meio de um deploy manual — mitigar com o `deploy.sh` parando o timer durante o deploy.)*
4. **✅ 🔧 LANDMINE DESARMADA (2026-07-07).** O auto-deploy `.github/workflows/deploy-prod.yml` fazia `docker run` de imagem ghcr com env INCOMPLETO (faltava `ENCRYPTION_KEY` etc.) num push de `backend/**`/`frontend/**`, e **parava o container bom antes** → quebraria prod sozinho. Já disparou no passado (falhava cedo — prod sobreviveu a ~30 gatilhos). **Desarmado:** `on: push` removido → só `workflow_dispatch` (manual); cabeçalho no arquivo documenta o porquê + como reativar com segurança. Arquivo **preservado como referência** (não apagado; `git revert` reverte). Merges de backend/frontend **não disparam mais** deploy automático → destrava (com segurança) os próximos merges de código. **Melhoria futura OPCIONAL (não urgente):** reativar como auto-deploy CORRETO — alinhar com o deploy manual (mesmo compose/env completo) + health-check pós-deploy com rollback + testar em staging.
5. **⬜ 🟡 🔧 F11 — rotacionar a senha admin** (`admin@loja.com`) — pré-requisito de produção real.

---

## 📋 Técnico — REGISTRADO PRA DEPOIS (backlog — referência, não cobrança)

**Segurança**
- 🟡 Bloco B: `JwtAuthGuard` global fail-open → **fail-closed**.
- ✅ **Audit log de login — CONSERTADO + DEPLOYADO + VALIDADO em prod (2026-07-07)** ([runbook](../superpowers/plans/2026-07-07-audit-log-rls-fix.md)). Causa raiz: `audit_log` tem **RLS FORCE**; o `AuditLogService.log()` do login `@Public` rodava **sem contexto de tenant** → RLS rejeitava o INSERT → **100% quebrado, não intermitente** (0 audits de LOGIN em 10d). **Invisível em teste** (specs conectam como superuser, que bypassa RLS). **Fix** (commit `0558d66`, no ar): o `AuditLogService` seta o próprio contexto RLS (`runInTransaction` + `set_config` do `tenantId`) — conserta login + **checkout público** + LGPD de uma vez + logging da causa na string (bônus: parou de logar email/PII). **TDD RED→GREEN sob papel `ucm_rls_probe`** (RLS-enforced, não superuser). **Deploy validado em prod:** rebuild+restart do backend (~6s downtime, health 200), login de teste → **LOGIN audit gravou (=1)**, regressão → **CREATE audit gravou (=1)**, dados de teste limpos. **Gap histórico irrecuperável (documentado):** logins de prod de ~2026-07-04 até o deploy **sem rastro de auditoria** — perda não recuperável.
- 🟢 **Debug verboso em prod — FIX PRONTO na branch `chore/cheap-wins` (aguarda deploy)** (achado via Ponta 3): ~8.6k linhas `"level":"debug"`/7d, fonte = `stock-sweeper` (`@Cron` a cada minuto, só contagens). **Diagnóstico (2026-07-10):** o "como" antigo (`setar LOG_LEVEL=info`) era **placebo — o backend NÃO lê `LOG_LEVEL`** (só a Evolution API o lê); o `StructuredLogger.debug()`/`verbose()` escreviam INCONDICIONALMENTE em prod e o `getLogLevels()` (o filtro) era **dead code nunca ligado**. **Fix (Option A, TDD 9 testes):** gate no `StructuredLogger` — debug/verbose só saem em prod com `LOG_LEVEL=debug` (silencioso por padrão, religável pra investigar) + `getLogLevels()` LIGADO como fonte única de nível. Fecha o ruído E o "risco de vazar detalhe". Código na branch (`8a13918`), **NÃO deployado** (pega carona no próximo deploy).
- ✅ **Camada 5 — `http-exception.filter` vazava a query string no `path` — CONSERTADO + DEPLOYADO + VALIDADO em prod (2026-07-08, merge `49ccc4b`).** O filtro punha `request.url` (com a query) no `path` da resposta 4xx/5xx **e** do log warn (a máscara só cobria ≥500, não o `path`). Fix: helper `sanitizePath` remove a query inteira (só o pathname; robusto — não denylist frágil; `requestId` correlaciona). TDD 7 testes (3 vazamento + 4 regressão). **Validado ao vivo:** webhook 403 com token errado → `path: /api/v1/whatsapp/webhook` limpo, `message` controlada, token **0 ocorrências** no log; mascaramento ≥500 intacto. *(O verify token que vazou antes NÃO foi rotacionado — risco baixo aceito, só serve pro handshake.)* Mesmo tema das Camadas 1-4 ([[security-pii-logs-fix]]).
- ✅ **Camada 5b — mensagem auto-404 do NestJS ecoava a query — CONSERTADO + DEPLOYADO + VALIDADO em prod (2026-07-08, merge `fddd252`).** Para rota **inexistente**, o NestJS gerava `"Cannot GET /url?query"` e a query vazava pelo **`message`** (resposta + log), não pelo `path` (esse já limpo pela 5). Fix: helper `sanitizeMessage` faz **substituição literal** da URL crua da requisição (`request.url`) pela versão sem query no `message` — fecha a CLASSE (qualquer message que ecoa a URL), não denylist; zero falso-positivo em mensagem de negócio; mantém rota+verbo pro debug. TDD 11 testes (7 do #1 + 4 da 5b). **Validado ao vivo:** GET em rota inexistente com `?token=…` → `message: "Cannot GET /api/v1/rota-inexistente-5b"` (sem query) na resposta E no log (query 0 ocorrências); #1 não regrediu (`path` limpo, mascaramento ≥500). Mesmo tema das Camadas 1-4/5 ([[security-pii-logs-fix]]).
- 🟢 `/whatsapp/test` **agora fail-closed em prod (403)** — provado empíricamente 2026-07-06 · ✅ `/whatsapp/metrics` **fail-closed** (`WHATSAPP_METRICS_API_KEY` obrigatória → 403 sem config, 401 com key inválida) — **verificado em prod `98e1827`, 2026-07-09**.
- 🟡 Vazamento cross-tenant **além do RLS** (cache Redis / logs / prompts do LLM) — auditar.
- 🟢 JWT em localStorage → httpOnly cookie (delicado; já mitigado por CSP+revogação+TTL).
- 🟢 Dead code: ~~`whatsapp.service.legacy.ts` (14.8k linhas) + `*.service.ts.disabled`~~ + branch `chore/preserve-server-snapshot` (arquivo morto — descartar após revisar `email/`, `subscriptions/`, `whatsapp-cart.controller`). **⚠️ CORREÇÃO (2026-07-09, provado pelo inventário de 10 seções): o `legacy.ts` e os `*.disabled` JÁ FORAM DELETADOS — não existem mais na árvore; o CLAUDE.md sec.7 ainda mente sobre isso (dívida de doc, ver [[whatsapp-doc-debt]]). Dead code REAL hoje = ~10k linhas DORMENTES em 4 ilhas (sales-intelligence 9 serviços ~6.300L, conversation-manager+interactive-message ~1.071L, ilha `utils/` ~2.875L, avulsos) + métodos órfãos em arquivos vivos — tudo compila e passa CI, nunca executa numa mensagem real.**
- ❓ Round-trip autenticado do envelope (verificação end-to-end pendente) · "rotação de senha fraca" (provável = F11).

**Auditoria empírica adversarial (2026-07-06)** — dossiê completo em [`docs/security/2026-07-06-auditoria-empirica.md`](../security/2026-07-06-auditoria-empirica.md). 7 blocos, ataques REAIS contra o teste. **Zero 🔴 explorável.** Achados de hardening 🟡 registrados aqui:
- 🟡 **H1** revogação **fail-open com Redis fora** (logout não revoga se o Redis cai; válido até expirar em 15min) — o mais "real"; mitigado por TTL curto.
- 🟡 **H7** `POST /payments/:id/confirm` é **staff-wide** (sem `@Roles('admin')`) e não verifica o provedor — OK no modelo dono-único; revisar se surgir staff de menor confiança.
- 🟡 **H2** timing-oracle ~70ms no login (enumeração de email; mitigável com dummy-hash) · **H3** enumeração via `send-confirmation` (campo `success`).
- 🟡 **H8** latência degrada sob alta concorrência (amplificado pelo túnel de teste; em prod há throttle 100/min + nginx edge; sem 5xx/queda) · **H9** `JwtAuthGuard` não-global ("aberto por padrão"; footgun p/ endpoint futuro — mesmo item do "Bloco B" acima).
- ✅ **H4/H5/H6 — FIXES PRONTOS na branch `chore/cheap-wins` (TDD, aguardando deploy):** **H4** body >limite → **413 não 500** (`4db0cfd`: o filtro respeita o `.status` do erro Express; limite 100kb mantido, adequado ao maior payload legítimo) · **H5** `@Get(':id')` **valida UUID → 400 antes do service** (`9f7c570`: `ParseUUIDPipe` em products/orders/payments; `tenants/:slug` e o `whatsapp-cart/:tenantId` já-validado ficaram de fora) · **H6** `quantity` **inteiro ≥1** (`7379ccc`: `@IsInt` no `CreateOrderItemDto` + guard no parser hand-rolled do cart, que aceitava 3.5/0/-1/'abc'; a `dto/cart.dto.ts` é dead-DTO, não tocada). **Follow-ups ✅ FECHADOS na branch (2026-07-10):** H5-mutação (`e1769fa`: `ParseUUIDPipe` em **9 rotas** — products update/remove/reserve/release/adjust-stock/min-stock, orders `:id/status`, availability delete, payments `:id/confirm`) · H6-update (`172c403`) — ver o achado abaixo. **Novo micro-follow-up 🟢:** `lgpd/solicitacao/:id` é um `@Get(':id')` que escapou do H5 original (mesmo padrão, aplicar quando conveniente).
- 📚 **APRENDIZADO (2026-07-10 — a régua "verifica na fonte" pegou uma instrução errada ANTES de aplicar):** no **update-cart, `quantity 0` = REMOVER o item** (intencional — `cart.service.ts:210` `if (quantity <= 0)` libera a reserva e tira do carrinho). Por isso o `parseAndValidateUpdateCartDto` valida **≥0, NÃO ≥1** como o add-parser; documentado no teste. Aplicar o "≥1" que foi pedido teria **quebrado a remoção-via-0** — a fonte disse o contrário da instrução.
- ⚪ **A1 — a-provar:** prompt-injection no bot **não testado** (OpenAI unset no teste → bot cai em canned). Rodar a bateria no LLM ao vivo **quando a chave OpenAI estiver no ambiente de teste**.
- ✅ **RESOLVIDO/ESCLARECIDO pela auditoria:** multi-tenant **provado sob ataque real** (não só RLS no código: cross-tenant read/write/IDOR/webhook-forge todos bloqueados) · Asaas webhook **fail-closed token provado** (403) + MP webhook usa metadata autoritativo (forja → ignored) · `/whatsapp/test` **fail-closed em prod** (403, acima) · segredos **ausentes do log** (JWT_SECRET/senha DB = 0 ocorrências) · injeção clássica limpa · fluxo de dinheiro à prova (preço/amount server-side, cupom clampado, race de estoque+idempotência seguras) · CSRF N/A hoje (Bearer puro). *(✅ **re-verificado 2026-07-09:** o webhook do **WhatsApp** é **fail-closed em prod** — 403 sem secret / sem assinatura / assinatura inválida, HMAC `timingSafeEqual` sobre o corpo cru. O `if (webhookSecret && signature)` que a CLAUDE.md §9 citava como fail-open é hoje só o branch de DEV; o fail-open real mora no `evolution-api.provider` **MORTO** — o controller não o chama.)*

**Infra** (F-items registrados em [server-infra-debt])
- ✅ **F12 — FEITO na branch `chore/cheap-wins` (`c94e6ff`, TDD):** o default público estava no **seed** (`seed-usuario-padrao.ts`, NÃO numa migration) — removido o fallback `|| 'senha123'` (o seed agora **falha com erro claro** sem `SEED_ADMIN_PASSWORD`, nunca cria admin com senha pública) + removido o `console.log` que **vazava a senha** no terminal/log; lógica extraída pra helper puro testável. **Follow-up ✅ FECHADO (`c8886b4`):** `test:scripts` no package.json + step no `ci.yml` → o spec do seed (fora do `rootDir: src`) agora roda no CI; F12 coberto lá também. *(F11 = rotacionar a credencial VIVA em prod = planejado-NÃO-executado; memória `f11-admin-password-rotation`.)* · **✅ F18 FECHADO na branch (`efdd9dd`):** o compose não passava `DATABASE_ADMIN_URL` pro container (`environment:` explícita, sem `env_file`) → `docker compose run backend migration:run` caía no `DATABASE_URL` (ucm_app, sem DDL) e a migration falhava; **passthrough adicionado** (inerte pro runtime, que usa `DATABASE_URL`; **ativa no dia de deploy**; exige `DATABASE_ADMIN_URL` no `.env` de prod).
- 🟡 Banco de teste (`ucm-postgres-test`/`redis-test`) rodando na VPS de **prod** (consome recurso).
- 🟡 VPS única = ponto único de falha (sem HA/failover/staging-espelho).
- 🔴 **LANDMINE nginx do HOST + OUTAGE (2026-07-08/09):** existe um `nginx.service` do **HOST**, **`enabled` (sobe no BOOT)**, que disputa a **:80** com o container `ucm-nginx` (o nginx real da prod, serve 80+443 c/ SSL/proxy) → corrida de porta no boot → docker nginx não binda → fica `Created` → **nada na :443 → prod DOWN**. Surfou nos testes da dívida F (o watchdog disparou auto-recovery, recriou containers, bateu no `address already in use`). **Recuperado (autorizado):** host nginx `stop`+`disable` + `docker compose up -d --force-recreate nginx` (⚠️ um `up -d` simples religa um container de **rede QUEBRADA** — `NetworkSettings.Ports` vazio + DNS `127.0.0.11` recusado; **só `--force-recreate`** reconstrói rede+portas). Prod restaurada (200/200, watchdog active/enabled, host nginx inactive/disabled). **(1) ✅ LANDMINE FECHADA (2026-07-09) via `sudo systemctl mask nginx.service`** → `is-enabled=masked`; provado: `systemctl start` **recusado** (`Unit is masked`, rc=1) — não sobe mais no boot/dependência/manual; reversível por `unmask`; prod intocada (200/200). **(2) PENDENTE: aplicar a dívida F em terreno limpo** (a corrida de :80 não existe mais). Detalhe em [server-infra-debt].
- ✅ **Dívida F (deploy.sh auto-blindar o watchdog) — APLICADA (2026-07-09), em terreno limpo (landmine masked).** `trap ensure_watchdog_running EXIT` + `trap 'exit N' INT/TERM/HUP`: religa o watchdog em QUALQUER saída (preserva o exit code via `rc=$?`, `set +e` no trap, confirma `is-active` e grita `ALERTA CRITICO` se não voltou); passo `0/4` para o watchdog (best-effort, após os pre-checks). Tira o stop/start manual de todo deploy. **Testes:** A 25/25 (stub: success/fail/INT/TERM/HUP) + B (prova no `ucm-watchdog.timer` REAL). **Validada ao vivo (2026-07-09) — a prova mais forte possível:** o deploy no-op FALHOU o health-check (por um gap do próprio deploy, ver item abaixo) e **MESMO ASSIM o trap religou o watchdog SOZINHO** (inactive capturado no `0/4` em t+2s → `watchdog RELIGADO e ativo` no log → active/enabled, `rc=1` preservado). A invariante "deploy que quebra não deixa o monitoramento cego" provada na prática.
- 🟠 **Recovery robusto (item do incidente) — `up -d` pode deixar (a) container de rede QUEBRADA e (b) nginx com UPSTREAM STALE.** **(a)** No incidente: `up -d` num container que falhou o start deixou `NetworkSettings.Ports` vazio + DNS recusado (só `--force-recreate` limpou). **(b) 🔥 INSTÂNCIA CONCRETA PROVADA (2026-07-09, deploy de validação do F):** o `up -d` recriou o backend (novo IP) mas **NÃO o nginx** (imagem inalterada) → nginx ficou com **upstream STALE** → **502 por 2min** → o **health-check do deploy FALHOU (rc=1) apesar do app estar 100% OK** (backend logou "Nest started"); o **watchdog recuperou** (`docker restart …nginx` re-resolveu → `recovery_api_ready`). **IMPLICAÇÃO NOVA E URGENTE:** o `deploy.sh` **deve reiniciar/refrescar o nginx após o `up -d`** (exatamente o que o recovery do watchdog já faz na `ucm-watchdog.sh:76`), senão **todo deploy que muda a imagem do backend reporta FALHA falsa + ~60s de 502** até o watchdog salvar. **Fix = restart targeted do nginx no deploy** (+ `--force-recreate` health-gated pro caso (a)); **NÃO** force-recreate cego (reinicia postgres/redis). Sobe de 🟡→🟠. Precisa design próprio, mas agora com dado concreto.
- ⚠️ **GUARD-RAIL — o terreno `deploy/watchdog/nginx` é campo minado (2 sustos consecutivos, 2026-07-08/09):** cada mudança nele expôs um problema latente DIFERENTE — o Teste B do watchdog → nginx-rogue do host (outage de :443); o deploy do F → nginx stale upstream (502). **Regra pra próxima sessão: qualquer mudança nesse terreno = ciclo DEDICADO diagnóstico→plano→teste, com cabeça fresca, NUNCA no fim de uma sessão longa. Não empilhar mudança sensível sobre mudança sensível.** O F fechou e está provado; o próximo item (refresh do nginx no deploy) merece começar do zero, descansado.
- 🚀 **PILHA "PRONTO-mas-NÃO-DEPLOYADO" (cresceu 2026-07-10) — espera um DIA DE DEPLOY DEDICADO:** prod segue em `98e1827` (sã, 200/200). À frente há **12 mudanças não-deployadas**: (a) **3 fixes de serviço na `main`** (retry-break `b13279e`, escalação enriquecida `e52178a`, remoção do `extractCatalogQuery` `9b25a99` — das 26) + (b) **9 fixes na branch `chore/cheap-wins`** (LOG_LEVEL `8a13918`, H4 `4db0cfd`, H5-get `9f7c570`, H6-add `7379ccc`, F12 `c94e6ff`, H5-mutação `e1769fa`, H6-update `172c403`, test:scripts/CI `c8886b4`, F18 `efdd9dd` — TDD onde aplicável; a branch tem **11 commits** = 9 fixes + 2 docs). **🔴 PRÉ-REQUISITO do deploy:** fechar PRIMEIRO o **🟠 nginx-refresh** (item acima), senão todo deploy que muda a imagem do backend bate no **502 + health-check falso-negativo** até o watchdog salvar. **É CAPÍTULO PRÓPRIO (terreno minado — ver o guard-rail acima), NÃO empilhar no fim de sessão:** ciclo dedicado nginx-refresh → depois o dia de deploy. Ordem sugerida: (1) fix nginx-refresh, testado; (2) merge `chore/cheap-wins`→main; (3) deploy main→prod com o nginx-refresh já no `deploy.sh`.
- 🟢 F19 (`run` sem `-T`) · F20 (backup path velho) · F6/F8/F10 (sweeper cron / scripts na imagem / migrations faltando) · F13 (logout zumbi) · `.env.bak-20260703` solto no `/opt/gtsofthub`.
- 🟢 **Migração `baseUrl`→`paths` no `backend/tsconfig.json` — só AO SUBIR pra TS 7.0.** O `baseUrl` é depreciado e "para de funcionar no TS 7.0" (o `typescript@next` já é 7.1.0-dev). **HOJE é inofensivo:** o build real (`tsc --noEmit` + `nest build`) dá **0 erros** e o `ignoreDeprecations:"5.0"` cobre. ⚠️ **NÃO aplicar** a sugestão `ignoreDeprecations:"6.0"` que o editor mostra — a TS 5.9.3 do projeto **REJEITA esse valor** (`TS5103`) → quebraria o build (testado 2026-07-10). Quando migrar pra TS 7.0: remover `baseUrl` e reescrever `paths` relativo ao próprio tsconfig (`"@modules/*": ["./src/modules/*"]`). **Extra-seguro: 0 arquivos usam os aliases hoje** (`@modules`/`@common`/`@config`/`@database`/`@/*` — tudo importa por caminho relativo) → `baseUrl`+`paths` são **vestigiais**, dá até pra APAGAR os dois. *(Contexto: os 116 "erros" que o editor do Gustavo mostrava eram ruído do Language Server — TS do editor mais nova que a 5.9.3 do projeto; resolvido fixando a TS do workspace em `.vscode/settings.json`.)*
- ✅ **Branches `fix/*` mergeadas — LIMPAS (2026-07-09).** Deletadas 5 locais da saga (`whatsapp-test-baseline`, `cart-service-spec-resurrection`, `deploy-watchdog-blindagem`, `exception-message-query-leak`, `log-hardening-and-minor-bugs`) + 9 remotas mergeadas no `origin` (as 4 da saga + 5 antigas: `payment-env-passthrough`, `whatsapp-bot-intelligence`, `whatsapp-flow`, `whatsapp-llm-assist`, `whatsapp-rls-fix`). Commits preservados na main; ponteiros descartados. *(Restam locais antigas feat/*/security/* — não tocadas, fora da saga.)*

**Produto / UX**
- 🎯 **IA-VENDEDORA — frente de DESENHO de arquitetura (a peça central do produto). Território 100% mapeado read-only (2026-07-09); esboço da arquitetura 5/5 COMPLETO (2026-07-09) — pronto pra virar plano de implementação. As 5 decisões fechadas: 1 Modelo B (tool-calling) · 2 trava §5 + riqueza do produto · 3 FSM de ferro fecha o pedido · 4 canibalizar a ilha · 5 instrumentação pra aprender. Detalhe completo em [[ia-vendedora-arquitetura]].** 4 investigações densas feitas: (1) mapa do cérebro do bot = **rules-first ~80% regra / ~20% IA**, NÃO IA-no-centro (funil de ~5 camadas de keyword antes da IA; IA = OpenRouter/gpt-4o-mini, 2 papéis: `routeMessage` classifica + `generateTextReply` texto livre só em answer_question); (2) custo = **centavos/mês rules-first, ~R$4/mês AI-led a 200 conversas** → custo NÃO é o gargalo na escala doceria; (3) inventário completo de 10 seções; (4) veredito da ilha `sales-intelligence` (~6.300L, 9 serviços) = **CANIBALIZAR** (nem religar nem apagar) — é especificação validada, não código pra religar: keyword-engine com **3 bugs congelados `it.skip`** + **violação multi-tenancy do branch `loucas_brigadeiro`** (fere invariante) + **NÃO resolve o §5**. **Tese-alvo:** LLM no comando + núcleo vivo (rules-first com grounding travado no pedido) + **a TRAVA DO §5 imposta como invariante** (IA recebe catálogo real mas NÃO pode inventar preço/estoque = desafio central). Manifesto de colheita + detalhe completo em [[ia-vendedora-arquitetura]]. **Achados de conserto que PRECEDEM a expansão:** 🔴 **resposta-vazia do action-executor** (8 actions ghosteiam o cliente — `stateTransition` descartado em `whatsapp.service.ts:2099`; hoje raro, vira CRÍTICO se a IA for pro centro; 0 teste) · 🟠 router voa cego (`catalogSummary`/`recentMessages` hardcoded vazios) · 🟡 trava dura anti-alucinação de preço no caminho conversacional · 🟡 FSM morto (`handleCollectionStage` "confirma" sem criar pedido) + timeout não limpa `context.checkout`.
- 🟡 Branding **"GTSoftHub" hardcoded (~60 ocorrências)** — separar contexto-PLATAFORMA (landing/legal/login = ok) do contexto-TENANT (checkout/loja = deveria ser a marca do lojista).
- 🟡 Notificação de **novo pedido ao lojista** (ele não sabe que vendeu sem olhar a tela).
- 🟡 Bot só **LOGA o envio** (não entrega — falta ligar o provider; atrás da Meta).
- 🟠 **Bot `select_payment` roteia pro `handlePayment` STUB** (chave PIX hardcoded `00000000000`, **não cobra de verdade**) — débito flagado na Fase 2 do roteamento IA-vendedora (Tipo A, branch `feat/ia-vendedora-stateTransition-routing`): roteia certo, mas a cobrança real só entra quando a **Fase 4 de pagamentos** ligar o `paymentsService`/PIX por-tenant (casa com "Estados de borda"/Asaas em Escala abaixo + o recebimento-por-lojista).
- 🔴 **Confirmação MANUAL de pagamento não tem botão na UI (saída (c) pendente) — registrado no Dia D, 2026-07-19.** A política por ator (`orders/order-status-transitions.ts`, nova na pilha do Dia D) torna `PENDENTE_PAGAMENTO → CONFIRMADO` **exclusiva do ator `payment`** — nem admin nem cliente marcam pago à mão (é a trava certa: só dinheiro real confirma). A via legítima manual **existe no backend** (`POST /payments/:id/confirm` → `confirmPayment` → `updateStatus(..., actor: 'payment')`) e **existe no `frontend/lib/api-client.ts:420`**, mas **nenhuma tela a chama** — zero callers. **Consequência:** pedido de WhatsApp pago **fora** do fluxo online (dinheiro ou cartão **na entrega** — justamente as opções 2 e 3 que o `handlePayment` oferece ao cliente) só vira `CONFIRMADO` pelo **webhook do Mercado Pago**; sem webhook, a lojista não consegue nem confirmar nem mandar pra produção — **só cancelar**. *(PDV é imune: venda de balcão nasce `ENTREGUE`, `orders.service.ts:315-317`.)* **Saída (b) JÁ APLICADA no Dia D** (commit desta pilha): removida a transição `confirmado` do mapa do admin em `frontend/lib/order-status.ts` → o botão que gerava **400** sumiu, `cancelado` preservado, RED→GREEN em `order-status.test.ts` (`🔒 admin NUNCA marca pago`). **Saída (c) = ligar `confirmPayment` numa tela do painel — ciclo próprio, NÃO feito.** **PRIORIDADE: antes de a doceria depender de pagamento-na-entrega via bot.**
- 🟠 **`cancel_order` — handler real com checagem de OWNERSHIP (furo de segurança).** Hoje o roteamento (Tipo C) **ESCALA honesto** (pede o número, encaminha à loja) e **NÃO cancela** — de propósito: o `ordersService.updateStatus` valida só o tenant, **não o dono** (`customer_phone`), então cancelar pelo número deixaria um cliente cancelar o pedido de OUTRO. Follow-up: handler que **resolve o pedido → verifica `pedido.customer_phone === customerPhone` → só então** chama `updateStatus(CANCELADO)` (que já faz release de reserva atômico + `assertStatusTransition`). Com teste de ownership + de transição. *(Não há hole vivo hoje — cancel_order não cancela; é requisito pra QUANDO o handler real for construído.)*
- ✅ **`start_checkout` (Fatia 4 da IA-vendedora) — CONSTRUÍDO + VALIDADO AO VIVO (2026-07-19, branch `feat/ia-vendedora-tool-calling`, na fila do Dia D; SEM merge).** O handoff do loop de IA pra FSM de ferro: molde Tipo A (rotear pro `handleCheckout` que já existia), **resposta 100% determinística** (zero narração de IA no handoff — fecha a questão do cinturão §5 por construção). 3 fios: `llm-router` (union + menu + heurística fechar-vs-adicionar) · `action-executor` (case no passthrough) · `routeBusinessAction` (case → `handleCheckout`). **A invariante D2 foi PROVADA EMPIRICAMENTE antes de ligar os fios** (`whatsapp.service.checkout-d2.spec.ts`: rejeição/neutro no `confirming_address` → `ordersService.create` NUNCA chamado; RED demonstrado por sabotagem temporária). **Validação ao vivo com o router real: ✅10 · 🚩0 · ⚠️0 · ℹ️5** — as 5 frases de fechar → `start_checkout` (conf 0.9-0.95); as 3 de adicionar → `process_order`; consulta não virou checkout.
  - 🟡 **FRONTEIRA HONESTA CONHECIDA (nomeada, sem ação por ora): frases curtas ambíguas — "é isso" (sozinho), "manda ver" — viram `start_checkout` com conf 0.9-0.95, NÃO `clarify`.** O router fecha checkout com alta confiança em sinal fraco. **Por que não é bug bloqueante:** a invariante D2 contém o dano — o pedido só nasce após confirmação DENTRO da FSM, então o pior caso é o cliente cair na coleta e escapar com "cancelar" (reversível, **não escreve dinheiro**). **Mitigação se incomodar cliente real:** endurecer a heurística do prompt do router pra exigir mais sinal antes de `start_checkout`. **NÃO aplicar agora** — sem dano observado, seria otimização especulativa.
  - 🟢 **Nota lateral do router (fora da Fatia 4, sem ação):** "qual o total?" → `check_order_status` (`order_number: null`) em vez de uma consulta de carrinho. Em prod é interceptado por keyword antes do LLM. Anotar no refinamento futuro do router.
  - ℹ️ **Fronteira de pagamento mapeada e coerente:** o router separa EXECUTAR de PERGUNTAR — "quero pagar" → `select_payment`; "como faço pra pagar" / "aceita pix?" → `answer_question`. As três são interceptadas por keyword antes do LLM em prod (o desenho atual).
- 🟢 **`collect_info` — enum morto: confirmar emissão ou remover.** Está no `RouterAction` type + no switch do executor, mas o **router NÃO a emite** (fora do menu do `buildSystemPrompt`) e `handleCheckoutStage` retorna null no ponto do fallback. Hoje roteia pro fallback amigável (case explícito, nunca silêncio). Decidir: ou o router passa a emiti-la (e aí precisa de destino real, provável início de coleta via `handleCheckout`), ou o enum sai (dead-code). Flagado na Fase 2 (Tipo C).
- 🟢 Reimprimir cupom do histórico (Fase 3 do cupom) · F14 link workspace no login (UX).
- 🟢 **Detecção de abuso é PT-BR only** (achado na frente das 26). As `ABUSE_PATTERNS` cobrem só profanidade PT (`merda`/`caralho`/`vsf`…); **xingamento em inglês NÃO é flagrado.** Pro público BR da doceria, provavelmente OK — **decisão consciente de feature, não bug.** Revisitar se o público mudar.
- 🟢 **CTA "veja o cardápio" removido** de 2 mensagens do bot (`buildWelcomeMessage` + a de out-of-stock do `error-handler`) — achado na frente das 26. Intencional? Vale restaurar pra guiar o cliente ao catálogo? Decisão de produto (o não_found ainda diz "Quer ver o cardápio?").

**Higiene de teste**
- 🟡 `ucm_test_motor` acumula SKU duplicado → testes de order/payment integration falham (colisão `idx_produtos_tenant_sku_unique`) — limpar/isolar o banco de teste entre rodadas.
- ✅ **Baseline de falhas em whatsapp/products — FECHADA (26→0) + MERGEADA na main (2026-07-09, fast-forward `e52178a`).** 679 verdes na main, build+lint limpos. **⚠️ `main > prod` NOS 3 FIXES DE SERVIÇO: os 3 fixes que rodam no bot — (1) `break` do retry não-transiente no `executeWithRetry`, (2) escalação enriquecida (`buildEscalationMessage` lista nomes de produto pro atendente), (3) remoção do método morto `extractCatalogQuery` — estão NA MAIN mas NÃO EM PROD (prod segue em `98e1827`; a doceria não roda esse código ainda). Chegam no PRÓXIMO deploy de código (decisão à parte, provável junto com outros itens). NÃO ESQUECER: main está à frente de prod nesses 3.** Número real do diagnóstico: **26 falhas / 5 suítes** (não 39/6 — roadmap velho); 654 passavam. Diagnóstico (3 frentes): **🅰️ mock drift** (ProductsService 8 = falta `StockEngineService` no TestModule, igual ao cart-spec; CatalogManager ~8 = shape do produto mockado desalinhado) → **2 fix-raiz cobrem 16**; **🅱️ teste desatualizado/copy** (ResponseBuilder 3, ErrorHandler-copy 3, extractCatalogQuery 1, MessageProcessor 2 — test-only, mas 3 de comportamento precisam de verificação antes); **🚨 1 bug de código real** (abaixo).
  - ✅ **`executeWithRetry` — bug do retry não-transiente CORRIGIDO (`b13279e`).** O loop retentava `maxRetries`(3)× mesmo em erro não-transiente (o `shouldRetry` só gateava o *delay*, não o *break*) → fix: `break` quando `!shouldRetry`, falha na 1ª. TDD (o RED já existia); transientes seguem retentando (verde). **Severidade real 🟢: o método é DORMENTE (0 call-sites em prod — só `handleError` é usado).** Consertado pra estar correto pra uso futuro — se um dia embrulhar um write, teria sido 🔴 (retry triplicaria o efeito).
  - ✅ **`extractCatalogQuery` — REMOVIDO (dead code, superado pela IA).** 0 call-sites (provado 2×, incl. string/reflexão); o roteamento real (`whatsapp.service.detectIntent` → `fallback` → `llmRouterService`) usa o cérebro de IA pra entender "quero X", não esse extrator por padrão. Diferente do `executeWithRetry` (utilitário genérico → consertado): esse foi substituído → removido (método + 3 testes). CatalogManager 17/17 verde.
  - ✅ **7 de copy/comportamento CORRIGIDOS (test-only, serviço certo):** ResponseBuilder (2: "demorei"→"Demorei" case; welcome "cardápio"→"pedido") + ErrorHandler (3: timeout "lenta"→"manutenção", AI-timeout "demorando"→"lenta", out-of-stock removeu "cardápio") + MessageProcessor (2: whitespace — serviço faz normalização MÍNIMA e PRESERVA estrutura de quebras, teste assumia colapso; abuso — input trocado pra PT-BR "merda", o detector é PT-BR por design).
  - 🚨 **#3 (`buildEscalationMessage`) — ACHADO SÉRIO, aguarda decisão de produto (NÃO consertado).** O handoff pro atendente humano inclui o **nome do cliente** + a **CONTAGEM** de itens (`Itens: N produto(s)`) mas **NÃO os nomes dos produtos**. O teste passa `pending_order.items:[{produto_name:'Brigadeiro'}]` (shape correto, serviço lê) e espera "Brigadeiro" — mas o serviço só emite a contagem. **NÃO é copy nem mock-drift: é gap de conteúdo — o atendente humano recebe "quantos" mas não "o quê" o cliente quer.** Decisão do Gustavo: enriquecer a escalação com os nomes dos produtos (fix de SERVIÇO, mais útil) vs aceitar contagem-só (atualiza o teste).
- ✅ **INVENTÁRIO DE CÓDIGO MORTO — feito (varredura consolidada read-only 2026-07-10) — PENDENTE de remoção item-a-item** → mapa completo com file:line em `dead-code-inventory` (memória). Varreu WhatsApp + products/orders/payments/auth/cart, classificado por RISCO DE REMOÇÃO: **🟢 ~19 mortos seguros** (métodos isolados: `releaseMultipleStocks`, `findLatestPendingByCustomerPhone`, `getPaymentStatus`, `extendTtl`, o quarteto do `conversation.service`, 5 builders do `whatsapp.service`, `searchProducts`, `buildFarewell`/`buildOrderSummary`…) · **🟡 ~9 a confirmar** (⚠️ `JwtAuthGuardProd` = guard que BYPASSA auth fora de prod = footgun; `applyCoupon`/`removeCoupon`; a `dto/cart.dto.ts` INTEIRA superada pelos parsers hand-rolled; os spec-only do `message-processor`; `executeWithRetry` = **MANTER**; a ilha `utils/` 27 arq) · **🔴 ~6 que escondem valor/bug** (ilha `sales-intelligence` = **Decisão 4 CANIBALIZAR**; FSM morto `handleCollectionStage` = **Decisão 3 remover** (fake-confirmation); `conversation-manager`; `interactive-message`; o AI dormente do `openai`). **products/orders/payments/auth quase limpos** (112 símbolos → só 4 métodos isolados). **Remoção = sessão futura, item-a-item, cabeça fresca; confirmar 0-call-sites NA HORA de deletar (régua "prova ao deletar").**
- ✅ **`cart.service.spec.ts` — RESSUSCITADO (2026-07-08, branch `fix/cart-service-spec-resurrection`).** Era FALSA COBERTURA: o `TestingModule` só provia 2 das **4** deps do `CartService` (faltavam `DataSource` + `StockEngineService`) → falhava no `beforeEach` → nenhum dos 13 testes rodava. Fix: providas as deps espelhando a infra real (o `DataSource.transaction` invoca o callback com um manager cujo `getRepository`→cart repo e `query` resolve o `set_config` RLS; `StockEngine` no-op; `repo.find` do `expireOldCarts`). **Suíte viva: 13/13 verde, exercitando a lógica real.** 🎁 **A cobertura morta escondia 1 teste mal-escrito (NÃO um bug de serviço):** "expired cart exists" asseria `repo.update`, mas o serviço expira por um **UPDATE cru atômico** via `manager.query` (`releaseExpiredCart`, compare-and-set idempotente) e o teste nunca semeava um carrinho expirado → corrigido pra testar o caminho real. **Sugestões de cobertura futura → registradas como item próprio abaixo.**
- 🟢 **Cobertura a expandir no `cart.service.spec.ts`** (aberto desde a ressurreição, 2026-07-08). Agora que a suíte roda de verdade, faltam testes DIRETOS de: `getCartByTenantAndPhone` (o método do BUG-CART-1 — hoje coberto só via o spec standalone do controller), `applyCoupon`, `updateItemQuantity`, `releaseExpiredCart` (a expiração atômica). Não é dívida (a suíte é honesta), é **cobertura a expandir pra quando fizer sentido** — bom candidato pra fortalecer o repo.

**Escala / consistência de dinheiro**
- 🟡 Reconciliação de pagamento: só há o **StockSweeper** (cancela não-pago no TTL → `paidForCancelled` → aviso de reembolso manual); **sem polling ativo** ("gateway, pagou?").
- 🟡 Estados de borda: MercadoPago reconhece chargeback/refund mas **sem fluxo de reembolso no app** (manual); **Asaas: nenhum estado de borda tratado** (cego pra go-live).
- 🟡 Tetos externos: Asaas 20 subcontas/dia (já batido), Meta tiers de mensagem, custo de **LLM por conversa × volume**; pool de conexões do Postgres em escala.
- ❓ Idempotência do webhook do **bot** (Meta re-entrega em retry — deduplica?) — verificar.

**Bugs conhecidos**
- ✅ **BUG-CART-1** (`GET .../cart` → 500 sem carrinho) **RESOLVIDO em prod** (`d0470c6` — retorna carrinho vazio, não 500; caminho `getCart`→`buildMinimalCartSummary`→`generateSummary` null-safe ponta-a-ponta; verificado na árvore de prod `98e1827`, 2026-07-09) · 🟡 **BUG-CART-2** (`/whatsapp/cart/add` → 401, lê `id` em vez de `sub`) — **NÃO verificado, segue aberto até prova** (latente atrás do bloqueio Meta).
- 🟢 logout 201→200 (cosmético).

---

## 💼 Decisão de negócio a fazer (dono, sozinho ou comigo)

- **⬜ Unit economics.** A mensalidade **cobre o custo por-tenant**? Somar: VPS + Asaas (R$0,99/cobrança) + LLM/mensagem + Meta. Se custo > fee, **perde-se dinheiro ao escalar.** Fazer a conta **antes** de vender pra 50.
- **⬜ Posicionamento vs Cardápio Web.** Qual o **diferencial** que justifica escolher você? (Inferência: bot WhatsApp com IA + PDV + estoque + **recebimento por-lojista** integrado, vs. cardápio/pedido online.) É decisão de **narrativa do dono** — precisa ser articulada, não só técnica.

---

## 📎 Apêndice — Inventário completo (as 3 camadas)

### Camada 1 — Estado real

**Em produção** (`origin/main` `0558d66`; servidor `/opt/gtsofthub` HEAD == main; site + `/api/v1/health` = 200):
- Multi-tenancy RLS (core).
- Segurança HIGH fechada: envelope encryption Fase 1 + auth Bloco A (logout com revogação por-token provada no ar, privesc do register fechado, política de senha, bcrypt 12, branding só-admin) + Blocos 1-3.
- PDV / venda de balcão (tela caixa + fast-pass).
- Admin — Produtos & Estoque + motor de estoque + configurações da loja + disponibilidade. *(✅ MERGEADA — a memória antiga dizia "não", estava velha.)*
- Pagamento no PDV (confirmpayment endurecido) + PIX no bot (provado local).
- Bot WhatsApp (fluxo/orquestrador) — **mas o envio só loga** (não entrega; ver Meta).
- Polimento 22 bugs (4 blocos).
- Observabilidade (watchdog HTTPS + netdata→Telegram + app-alert Tier 1-lite + backup automático 03:00 BRT) + correção do audit-log de login (RLS, `0558d66`).
- **Fix de PII nos logs (LGPD) — DEPLOYADO 2026-07-07 (`79ff822`):** parou de logar o **conteúdo da mensagem** do cliente (Camada 1) + **mascara telefone/email** em ~17 sites (`maskPhone`/`maskEmail`, Camada 2) + auditoria **estruturada + IP mascarado** no cart-controller (Camada 4 — **eventos de segurança agora chegam no app-alert**) + máscara do telefone na **exceção** de número não-autorizado (que reaparecia cru via o http-exception-filter). *(Camada 3 — payload MercadoPago — verificada limpa, sem fix.)* **Provado vivo no container** (leak velho `[DEBUG handleCart]` sumiu, máscaras compiladas presentes, exceção mascara). Fechado **ANTES** do bot ir pra prod — o vazamento que ligaria junto está estancado. Watchdog blindado no deploy (parado/religado). Reversível (`git revert 79ff822`).

**Construído mas não integrado (branches abertas):**
| Branch | Contém | Provado? | Espera |
|---|---|---|---|
| `feat/pdv-cupom-venda` (7c) | Cupom não-fiscal Fase 1 (builder + CupomVenda + portal + botão + store_name) | ✅ 107 testes | Teste na bobina real + preview PDF do dono |
| `feat/asaas-phase1-subaccount` (19c) | **Frente Asaas inteira** (⊃ `payments-neutral`): fundação neutra + Fase 1 (subconta escopada + 🎯 Risco A) + Fase 2 (PIX + roteamento reverso + **🎉 Rota 1 PROVADA AO VIVO com subconta REAL 2026-07-07**: 400→200, QR, roteamento reverso, Risco A 403) | ✅ **sandbox ao vivo** | 🔴 Gate Fase 4 (CNPJ+prod + migration + credenciais por-tenant + KMS + revisão adversarial) — **NÃO mergear antes** |
| `feat/payments-neutral-foundation` (7c) | Fundação neutra sozinha (subconjunto da de cima) | ✅ | Entra junto com a `asaas-phase1` |
| `feat/whatsapp-cloud-api-webhook` (1c) | Webhook WhatsApp Cloud API (Meta): verify + parser + HMAC | ✅ código | **Meta ✅ criada** — falta ativar em prod + teste real |
| `chore/preserve-server-snapshot` (1c) | Arquivo histórico da faxina (código server-only) — **NÃO é pra merge** | — | Descartar após revisão item-a-item |

**Bloqueado por mundo real:** **Asaas ✅ chamado 1356594 resolvido — PIX de subconta destravado + Rota 1 provada ao vivo (falta CNPJ+prod=Fase 4)**, **Meta ✅ conta criada — bot destravado, falta ativar em prod**, CNPJ (não aberto) — ver seção "Ações do mundo real".

#### 🔀 Decisão de merge (2026-07-07) — o que entra agora vs. o que espera gate
Diagnóstico das branches abertas (a régua "verifica antes de agir"): todas partem da mesma base `a10b58a`; a main andou 6 commits (audit-fix + observabilidade) **sem conflitar** com nenhuma.
- **✅ MERGEADA AGORA:** `docs/roadmap-vivo` — doc pura (0 código, 0 toque em prod, sem conflito, não espera nada). Trouxe roadmap + runbooks + dossiê da auditoria pro tronco.
- **Pilha real:** `feat/asaas-phase1-subaccount` **contém** `feat/payments-neutral-foundation` (empilhadas — entram juntas). `feat/pdv-cupom-venda` e `feat/whatsapp-cloud-api-webhook` são **independentes** (não carregam pagamentos).
- **🚨 Correção de leitura — os pagamentos NÃO são dormentes:** o `payments.service` na branch **re-roteia o MP** pelo `credentialResolver.resolveForCharge('mercadopago', tenant)` (tabela nova `tenant_payment_credentials`, **fail-closed sem credencial — nunca fallback pro env**). **Deployar isso sem a migration + a credencial por-tenant QUEBRA o PIX da doceria.** Por isso a fundação de pagamentos (Fase 0/1/2, **provada em sandbox**) é **🔴 pré-requisito da Fase 4 (não só "aguarda gate"): NÃO pode ir pra main antes** de a Fase 4 tratar migration + credenciais por-tenant + a transição do MP existente (CNPJ + KMS + revisão adversarial). Merge ≠ deploy (deploy é manual), mas deixá-la na main viraria bomba pro próximo deploy manual.
- **✅ Landmine `deploy-prod.yml` DESARMADA (2026-07-07):** `on: push` removido → só `workflow_dispatch` manual. Merges de backend/frontend não disparam mais auto-deploy (provado: o merge do fix de PII em backend não disparou).
- **Gates dos que ficam:** cupom → teste na bobina real da mãe · pagamentos → **Fase 4 (CNPJ+prod; PIX de subconta ✅ destravado + Rota 1 provada AO VIVO)** · bot → conta Meta (✅ criada) · `chore/preserve-server-snapshot` → descartar após revisão.

#### 💳 Fase 4 — pagamentos em PRODUÇÃO (o caminho que resta, pós-Rota-1-provada)
A Fase 2 (Rota 1) está **provada ao vivo em sandbox** (round-trip PIX real + roteamento reverso + Risco A). Pra ligar pagamentos com lojistas **reais** falta:
1. **👤 CNPJ real** (contador) — a conta-plataforma Asaas de produção **EXIGE CNPJ** (MEI vs empresa: ver pauta do contador).
2. **👤 Conta Asaas de PRODUÇÃO** — subcontas de lojista aprovadas por **KYC/documentos reais** (não o atalho `/sandbox/approve`; em prod é o lojista que envia docs via `onboardingUrl`).
3. **🔧 Migration `1752200000000`** (`tenant_payment_credentials`) rodada em prod.
4. **🔧 Migrar a doceria pras credenciais por-tenant** — hoje o MP é global (env); a Fase 4 move pra credencial por-tenant no cofre + a transição do MP existente **sem quebrar o PIX atual** (o `payments.service` já re-roteia fail-closed → por isso não pode ir pra main antes disto).
5. **🔧 Merge da fundação de pagamentos** (`feat/asaas-phase1-subaccount` ⊃ `payments-neutral`) — 🔴 **segura na branch ATÉ aqui**; mergear antes quebra o PIX da doceria.
6. **🔧 KMS** — a `ENCRYPTION_MASTER_KEY` sai do `.env` pro cofre gerenciado (rotação).
7. **🔧 Revisão adversarial** do fluxo de pagamento em prod (dinheiro real de terceiros).
- **🔎 Achado pra Fase 4 (2026-07-07):** checar se um lojista foi aprovado usa a **chave-plataforma** (`GET /accounts` lista subcontas + status), **NÃO** a chave escopada da subconta (que dá **403 `ACCOUNT_INFO:READ`** — o escopo "só cobrança" não lê dados de conta).

**Servidor (só-leitura, 2026-07-05):** VPS `vps-0e3446f6` (up 23d). Containers: frontend/backend (26h) · nginx (40h) · postgres/redis (saudáveis) · **+ postgres-test/redis-test** (banco de teste `ucm_test_motor` na VPS de prod). Git `/opt/gtsofthub` == `origin/main` limpo (só o `.env.bak` untracked). **Sem divergência de código.**

### Camada 2 — Backlog conhecido
Ver **"Técnico — registrado pra depois"** acima (é a mesma lista, já agrupada por tema com gravidade). Origem: dívidas registradas ([server-infra-debt], [security]), git, docs.

### Camada 3 — Pontos cegos
Ver **"Técnico — atacar em breve"** (os 🔴) + **"Escala / consistência de dinheiro"** (técnico) + **"Ações do mundo real"** e **"Decisão de negócio"** (mundo-real). Os que **não estavam no radar**: KYC/AML, unit economics, continuidade de fornecedor (Meta bane número; Asaas encerrar = todos param), fator ônibus (solo founder), vazamento cross-tenant além do RLS.

---

## Como usar / atualizar este doc
- **Concluiu um item?** troque `⬜`→`✅` (ou `🔄`) e data.
- **Nova frente?** entra em "Atacar em breve" ou "Registrado pra depois" com dono (👤/🔧) + gravidade.
- **Destravou um bloqueio de mundo-real?** move o item e destrava a(s) frente(s) que ele libera.
- Fontes de verdade: git (branches/prod) · `docs/CONSOLIDADO/` · memória do agente.
