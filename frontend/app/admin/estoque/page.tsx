'use client';

import { StockManager } from '@/components/admin/StockManager';

/** Página de estoque. A casca (AdminShell) fornece o layout e o auth-gate. */
export default function EstoquePage() {
  return <StockManager />;
}
